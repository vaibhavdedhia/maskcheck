"""maskcheck -- diagnose value-level damage from constrained decoding.

The core (path tracking, scoring, repair, displacement) has no third-party
dependencies and runs on Python 3.9, so it installs anywhere. Engine
adapters are optional extras.
"""

__version__ = "0.1.0"
